sudo service redis-server restart      --> to start the ubuntu.
[sudo: authenticate] Password:         -->it will ask for an password enter the password.
  redis-cli ping                       --> to test wether its running or not
PONG                                   -->it will give res 
 
